# Calculator
This Python project is a basic menu-driven calculator that uses functions to perform addition, subtraction, multiplication, division, and modulus. It repeatedly displays a menu, takes user input, processes the selected operation, and exits when the user chooses.
